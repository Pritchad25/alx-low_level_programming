#include "main.h"

/**
 * add - Adds two integers and returns
 * the resutlt
 *
 * @a: the first number
 * @b: the second number
 * Return: the addition of both
 */
int add(int a, int b)
{
	return (a + b);
}
