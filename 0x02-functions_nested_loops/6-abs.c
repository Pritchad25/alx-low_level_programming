#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 * @t: The integer whose absolute value is to be computed.
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
	{
		return (t);
	}
	else
	{
		return (-t);
	}
}
